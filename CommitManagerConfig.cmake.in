set(_comp_file "${CMAKE_CURRENT_LIST_DIR}/CommitManagerClientTargets.cmake")
if(TARGET commitmanager-client)
    set(CommitManager_FOUND True)
elseif(EXISTS ${_comp_file})
    include(${_comp_file})
    set(CommitManager_FOUND True)
else()
    set(CommitManager_FOUND False)
endif()
